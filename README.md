Ze względu na wariant ekspresowy opisałem jedną metodę (getAllValidNeighbors) i po krótce jedną klasę (BFSProcessor). Jednak najpierw trzeba było zmienić minimalnie funkcję load() w klasie DataInput.
